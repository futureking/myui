import React from 'react';
import Button from '../index';

const ButtonStory = () => {
  
  return (
    <div>
      <Button type="primary" />
      <Button type="small" />
      <Button type="large" />
    </div>
  )
}